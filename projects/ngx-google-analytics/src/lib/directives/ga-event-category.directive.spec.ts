import { GaEventCategoryDirective } from './ga-event-category.directive';

describe('GaEventCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new GaEventCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
