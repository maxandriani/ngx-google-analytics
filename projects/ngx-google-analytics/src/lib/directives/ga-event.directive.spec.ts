import { GaEventDirective } from './ga-event.directive';

describe('GaEventDirective', () => {
  it('should create an instance', () => {
    const directive = new GaEventDirective();
    expect(directive).toBeTruthy();
  });
});
